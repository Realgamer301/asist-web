{
    "name": "assistant-attendance-system",
    "version": "1.0.0",
    "description": "GPS-based Assistant Attendance Tracking System",
    "private": true,
    "scripts": {
        "dev": "cd backend && npm run dev",
        "start": "cd backend && npm start",
        "build": "echo 'Build completed'",
        "install:all": "npm install && cd backend && npm install"
    },
    "dependencies": {
        "bcryptjs": "^2.4.3",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "jsonwebtoken": "^9.0.2",
        "mysql2": "^3.6.5",
        "selfsigned": "^4.0.0"
    },
    "devDependencies": {
        "nodemon": "^3.0.2"
    },
    "engines": {
        "node": "18.x"
    }
}
